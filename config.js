/* Global runtime config for web + native APK build.
   Native build script also reads this value and injects it into Android app. */
window.CINEMATIC_CONFIG = window.CINEMATIC_CONFIG || {
  apiBase: '',
};
